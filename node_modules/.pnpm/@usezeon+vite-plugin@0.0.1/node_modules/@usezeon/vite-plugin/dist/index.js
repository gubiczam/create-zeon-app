// src/index.ts
import path from "path";
import { fileURLToPath } from "url";
function rootDir(metaUrl) {
  return path.resolve(path.dirname(fileURLToPath(metaUrl)), "../../..");
}
function xyzPlugin() {
  const root = rootDir(import.meta.url);
  const alias = {
    "@xyz/core": path.join(root, "packages/core/src/index.ts"),
    "@xyz/runtime": path.join(root, "packages/runtime/src/index.ts"),
    "@xyz/router": path.join(root, "packages/router/src/index.ts"),
    "@xyz/ssr": path.join(root, "packages/ssr/src/index.ts")
  };
  return {
    name: "xyz-plugin",
    config() {
      return { resolve: { alias } };
    }
  };
}
var index_default = xyzPlugin;
export {
  index_default as default,
  xyzPlugin
};
//# sourceMappingURL=index.js.map